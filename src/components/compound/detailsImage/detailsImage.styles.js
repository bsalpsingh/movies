import { makeStyles } from "@material-ui/core/styles";
export const useStyles = makeStyles({
  root: {
    width: 340,
    margin: "auto",
  },
  media: {
    objectFit: "cover",
    width: "auto%",
    height: 500,
  },
});
